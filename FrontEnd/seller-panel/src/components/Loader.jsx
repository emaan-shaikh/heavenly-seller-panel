import React from 'react';
import '../styles/Loader.css';  // CSS for the loader

const Loader = () => (
  <div className="loader-overlay">
    <div className="loader"></div>
  </div>
);

export default Loader;
